[package]
name = "hhm-dioxus-web"
version = "0.1.0"
edition = "2024"
publish = false

[package.metadata.product]
stack = "Dioxus with shared interfaces"

[dependencies]
axum = "0.8"
tokio = { version = "1", features = ["macros", "net", "rt-multi-thread"] }
